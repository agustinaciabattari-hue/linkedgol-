[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm --filter @workspace/linkedgol build && pnpm --filter @workspace/db run push"

[deploy]
startCommand = "pnpm --filter @workspace/api-server start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

