[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @workspace/linkedgol build",
  "pnpm --filter @workspace/db run push"
]

[start]
cmd = "pnpm --filter @workspace/api-server start"
