[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x"]

[phases.install]
cmds = ["pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/linkedgol build"]

[start]
cmd = "pnpm --filter @workspace/db run push && pnpm --filter @workspace/api-server start"
